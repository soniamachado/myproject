/*o que é o html?
hypertext markup language
estrutura básica do html (<!DOCKTYPE html>, <html>, <head<, <body>).
incluindo metadados no <head>

elementos essenciais:
  cabeçalhos (<h1> a <h6>).
  Parágrafos(<p>)
  links (<a>)
  Imagens (<img>)
  listas (<ul>,<ol> e <li>)

Estruturando o conteúdo com <div>, <section>,<header>(trata-se do cabeçalho), e <footer> => todos estes ficam dentro do body
footer é o rodapé e o header é o titulo da página